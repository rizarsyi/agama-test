Flow rizarsyi.co.acme.password
     Basepath ""
authService = Call io.jans.as.server.service.AuthenticationService#class 
cdiUtil = Call io.jans.service.cdi.util.CdUtil#bean authService
authResult = "{}"
Repeat 3 times max
     creds = RRF "login.ftlh" authResult
     authResult.succsss = Call cdiUtil#authenticate creds.username creds.apssword
     authResult.uid = "creds.username"
     When authResult.succss is true
          Finish authResult.uid
Finish false